<Page
    x:Name="Root"
    x:Class="Slack.Views.MainPage"
    DataContext="{Binding Source={StaticResource MainViewModel}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Slack.Views"
    xmlns:common="using:Slack.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="ChannelDataTemplate">
            <Grid>
                <TextBlock>
                    <Run Text="#"/><Run Text="{Binding Name}" />
                </TextBlock>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" MinWidth="100"/>
                <ColumnDefinition Width="5*" MinWidth="300"/>
                <ColumnDefinition Width="16*" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Background="#362432"/>

            <Grid Grid.Column="1" Background="#51354b">
                <ListView ItemsSource="{Binding}" DataContext="{Binding Channels}" ItemTemplate="{StaticResource ChannelDataTemplate}"/>
            </Grid>

            <Grid Grid.Column="2" Background="White"/>
        </Grid>
    </Grid>
</Page>
